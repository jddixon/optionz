#!/usr/bin/env bash

if [ ! -z "${DEV_BASE// }" ]
then
  cd $DEV_BASE/py/optionz
  
  blListGen \
    -d ../optionz \
    -1L  \
    -u $DVCZ_UDIR \
    -X cover \
    -X .ssh \
    $@
  
else
  echo "DEV_BASE is not defined"
fi
